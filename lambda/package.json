{
  "name": "workflow-invoker-lambda",
  "version": "1.0.0",
  "description": "Lambda function to invoke Step Functions workflow",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean"
  },
  "dependencies": {
    "@aws-sdk/client-sfn": "^3.470.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}