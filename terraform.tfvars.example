# AWS Configuration
aws_region = "us-east-1"
project_name = "workflow-orchestrator"

# Docker Hub Credentials
# Create a personal access token at: https://hub.docker.com/settings/security
dockerhub_username = "your-dockerhub-username"
dockerhub_password = "your-dockerhub-token"

# Docker Images for Services
# For demo: Use the same image for all three services (they differentiate via SERVICE_NAME env var)
# Build once: docker build -t your-dockerhub-username/step-functions-demo:latest example-service/
service1_docker_image = "your-dockerhub-username/step-functions-demo:latest"
service2_docker_image = "your-dockerhub-username/step-functions-demo:latest"
service3_docker_image = "your-dockerhub-username/step-functions-demo:latest"

# Alternative: Use separate images if you want different functionality later
# service1_docker_image = "your-dockerhub-username/service1:latest"
# service2_docker_image = "your-dockerhub-username/service2:latest"
# service3_docker_image = "your-dockerhub-username/service3:latest"

# Optional: Customize container ports (default: 3000)
# service1_container_port = 3000
# service2_container_port = 3000
# service3_container_port = 3000